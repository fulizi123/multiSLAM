# 2026-03-15 对象链路推进与当前阶段总结

## 一、范围说明

这份记录主要总结：

- 相比 [`NEXT_SESSION_PLAN_2026-03-14.md`](/data/code/LL_SLAM_MultiCamera/NEXT_SESSION_PLAN_2026-03-14.md) 中“昨天的预期目标”，今天实际已经落地的对象相关改动
- 本次工作中已经验证通过的部分
- 仍然需要继续推进和回归确认的部分

重点范围放在：

- `ObjectObservation -> Frame/KeyFrame -> MapObject -> Viewer/mp4 -> LocalMapping/Optimizer`

不重复展开昨天已经完成的共视图、Local BA、MapPoint/KeyFrame culling 等基础 SLAM 结构内容，只在和对象链路直接相关时提及。

## 二、预期目标

昨天的预期目标，核心上是以下几个阶段：

### 1. 先把对象输入和可视化跑通

目标包括：

- 先验证 Viewer 中对象可视化是否真的可见
- 检查对象数量、静态/动态颜色、显示位置是否正确
- 优先确认数据和坐标系没有问题

### 2. 再进入对象地图层

原计划是：

- 在可视化验证通过后，再做 `MapObject`
- 建立 `Frame / KeyFrame` 到 `MapObject` 的关联
- 做静态 / 动态 / unknown 的后续管理基础

### 3. 最后才考虑对象优化

昨天计划中的顺序建议是：

1. 先读入
2. 再可视化
3. 再对象地图
4. 最后才进优化

## 三、关键进展与实现效果

今天的实际进展，已经明显超过了昨天“只先验证可视化”的范围，已经推进到了“对象地图层 + 基础对象优化 + Viewer/mp4 问题定位修正”。

### 1. 对象已经真正进入地图层，不再只是当前帧显示

今天已经完成了基础版 `MapObject` 链路落地：

- 新增 `MapObject` 数据结构
- 在 `Map` 中增加对象容器、`track_id -> MapObject` 检索
- 在 `KeyFrame` 中增加对象观测关联
- 在 `LocalMapping` 中按 `track_id` 创建或更新对象地图项
- 局部地图刷新时，静态对象已经可以作为局部对象集合参与后续处理

涉及的核心文件包括：

- [`include/Map.h`](/data/code/LL_SLAM_MultiCamera/include/Map.h)
- [`include/MapObject.h`](/data/code/LL_SLAM_MultiCamera/include/MapObject.h)
- [`src/Map.cc`](/data/code/LL_SLAM_MultiCamera/src/Map.cc)
- [`src/MapObject.cc`](/data/code/LL_SLAM_MultiCamera/src/MapObject.cc)
- [`src/LocalMapping.cc`](/data/code/LL_SLAM_MultiCamera/src/LocalMapping.cc)
- [`src/KeyFrame.cc`](/data/code/LL_SLAM_MultiCamera/src/KeyFrame.cc)

实现效果：

- 对象不再只存在于 `Frame::mvObjectObservations`
- 静态对象在观测跨关键帧后，已经具备“持久化到地图中”的基础能力
- Viewer 中已经可以区分“当前帧对象观测”和“地图中的静态对象”

### 2. 对象优化已经提前接入，但当前还是基础版

虽然昨天的建议是“最后再进对象优化”，但今天实际上已经接入了一个基础版对象优化：

- 在 [`include/Optimizer.h`](/data/code/LL_SLAM_MultiCamera/include/Optimizer.h) 中增加了：
  - `Optimizer::OptimizeLocalObjects(KeyFrame*, Map*)`
- 在 [`src/LocalMapping.cc`](/data/code/LL_SLAM_MultiCamera/src/LocalMapping.cc) 中，当前流程已经变成：
  - 先创建/更新 `MapObject`
  - 再做 `LocalBundleAdjustment`
  - 再做 `OptimizeLocalObjects`

当前这版对象优化的特征是：

- 只处理静态对象
- 只使用多关键帧对象观测做位置和朝向的平滑更新
- 还没有把对象顶点真正并进 `Local BA` 图里联合优化
- 本质上属于“后端基础版对象姿态整理”，不是完整的对象 BA

实现效果：

- 静态对象不会每次都被最新关键帧直接重置姿态
- 静态对象姿态已经具备跨关键帧一致化能力
- 为后续“对象作为语义地图元素”打下了基础

### 3. Viewer/mp4 不再只是对象计数，已经有地图侧和图像侧对象显示

今天的 Viewer 实际已经推进到比昨天计划更完整的状态：

- 中央 BEV 区域中可以画：
  - 地图中的静态对象
  - 当前帧中的对象观测
- 周围多相机预览图中可以画：
  - 当前帧对象的 3D cuboid 投影
  - `track_id`

涉及的核心文件：

- [`src/Viewer.cc`](/data/code/LL_SLAM_MultiCamera/src/Viewer.cc)
- [`include/Viewer.h`](/data/code/LL_SLAM_MultiCamera/include/Viewer.h)

实现效果：

- mp4 中已经不仅能看到对象数量文本
- 还能看到 BEV 中的对象轮廓和朝向箭头
- 相机预览图中也已经能直接看到对象 3D 框投影

### 4. 已经定位并修复“观测结束后车辆朝向突变”的核心问题

这次最关键的问题定位结果是：

- 问题不只是“框没画出来”
- 真正的主因是：静态对象从“当前帧观测显示”切换到“地图中的 `MapObject` 显示”之后，姿态约定和更新方式存在不一致

具体表现为：

- 一些本应朝左/右的车
- 在观测结束后
- 会突然显示成朝前/后
- 同时由于图像侧只画当前帧观测，因此 2D 框也会消失

今天已经完成的修正包括：

#### 4.1 修正 Viewer 中 cuboid 的局部轴约定

把对象框角点构造和 heading 箭头方向统一到了当前对象数据实际使用的 box pose 坐标约定下。

效果：

- 地图中静态对象的朝向显示不再和当前观测方向打架
- 左右朝向对象不会因为显示层坐标约定错误而被画成前后朝向

#### 4.2 修正静态对象姿态更新方式

原先的对象优化在更新静态对象时，会把姿态压成纯 yaw 再重建 quaternion，这会破坏对象原本的姿态基准。

今天改成了：

- 只在当前姿态基础上叠加平滑的 yaw 增量
- 不再从零重建一个丢失基准对齐的 quaternion

效果：

- 静态 `MapObject` 在观测结束后不会再明显跳成错误朝向
- 地图对象和当前帧对象之间的姿态衔接更稳定

### 5. 真实链路已经编译并跑通，短序列验证结果正常

今天已经完成了真实程序链路验证：

```bash
./../Examples/stereo_carla_multi ../Carla.yaml
```

验证结果：

- 成功编译并运行
- 已经跑通 100 帧短序列
- 正常生成 mp4 输出
- 运行日志中 `OptimizeLocalObjects` 已经在后续关键帧上实际触发
- 从短序列肉眼检查结果看，之前那个“左右朝向变前后”的问题基本已经消失

当前状态：

- 短序列验证通过
- 仍需要做 1200 帧完整回归确认

### 6. 一个需要特别记录的现实情况：当前实际输入不是 ObjectTrackTruth，而是 ObjectTrack

虽然昨天的预期目标里强调了 `ObjectTrackTruth` 作为离线 GT 输入，但当前这次真实运行的数据目录中只有：

- `ObjectTrack/`

没有：

- `ObjectTrackTruth/`

因此当前程序实际走的是：

- `ObjectTrack`

这意味着：

- 今天修复和验证的是“当前真实运行链路”的对象问题
- 但“基于离线 GT 真值对象输入”的完整验证，还没有在当前这份 sequence root 上真正发生

这件事后面需要单独处理，否则“以为自己在验证 GT，其实跑的是 tracking 输出”会影响后续判断。

## 四、当前阶段实现效果总结

相比昨天“先只把可视化做清楚”的预期，今天实际已经达成：

- 对象输入链已经稳定进入 `Frame / KeyFrame / Map`
- 已经有基础版 `MapObject`
- 已经有静态对象的地图持久化
- 已经有基础版对象后端优化
- 已经有图像侧和地图侧对象显示
- 已经定位并修掉 mp4 中最明显的对象朝向错误问题

但当前也要明确：

- 对象优化还不是完整 BA，只是基础版平滑/整理
- 图像侧 2D/3D 框依然只画当前帧观测，不会自动继承地图对象
- 动态对象目前还没有进入真正的地图层管理
- 当前对象输入源和“预期使用 GT”的目标还没有完全统一

## 五、下一步工作

### 1. 先完成 1200 帧完整回归验证

这是接下来最优先的事情。

重点检查：

- 之前修掉的朝向问题在长序列中是否稳定不复现
- 是否存在只在后半程才出现的对象姿态漂移
- mp4 中是否还有某些 `track_id` 会在观测结束后异常跳变
- Viewer 中相机侧 3D 框和 BEV 中地图对象是否长期一致

### 2. 明确当前对象输入到底要走哪一条链

需要尽快统一：

- 是继续先用 `ObjectTrack/`
- 还是回到昨天计划里的 `ObjectTrackTruth/`

建议：

- 如果当前目标是先验证系统链路和显示逻辑，可以继续用 `ObjectTrack/`
- 如果当前目标是严格验证坐标、姿态和对象地图逻辑，最好切回 `ObjectTrackTruth/`

### 3. 判断是否要让静态对象参与前端位姿约束

这是下一阶段很值得做、但要谨慎推进的一步。

建议顺序是：

- 前端先只把静态对象作为“辅助当前帧 pose”约束
- 不要把 `MapObject` 的地图状态 ownership 直接前移到前端
- 让对象地图状态仍然主要由后端维护

原因是：

- 前端频率高，但观测更噪
- 后端频率低，但更适合维护稳定地图状态

### 4. 增加对象级调试与回放工具

如果后面还要继续排查对象问题，建议增加：

- 指定 `track_id` 的日志追踪
- 指定帧区间的对象状态导出
- 对象“当前观测姿态 vs 地图姿态”的并排调试输出

这会极大降低后续定位成本。

## 六、系统下一步可完善方向与优先级

下面给出我对这套系统后续完善方向的建议优先级。

### P0：必须优先处理

#### 1. 1200 帧完整回归与问题复核

原因：

- 当前短序列通过不代表长序列稳定
- 先确认修复不是“只在前 100 帧正常”

#### 2. 统一对象输入源

原因：

- 现在“预期目标”和“实际运行数据源”不一致
- 会直接影响后续所有判断和论文式结论的可信度

#### 3. 做 ego-pose 精度回归检查

原因：

- 这两天仓库不只改了对象，还改了 Local BA、状态同步、局部地图选择、关键帧频率
- 如果轨迹精度下降，需要尽快做 ablation，把对象问题和 SLAM 主链问题分开

### P1：高优先级，建议下一阶段推进

#### 4. 让静态对象以“固定地图地标”的方式参与前端 PoseOptimization

建议形式：

- 先只优化当前帧位姿
- 静态对象地图项先固定
- 先做最小可用版本的对象残差

原因：

- 这是最有机会给相机位姿带来收益的一步
- 同时又不会把地图状态噪声直接放大到前端

#### 5. 完善静态 / 动态 / unknown 三态对象管理

当前系统中：

- 静态对象已经开始进入地图
- 动态对象基本仍然只停留在观测层

下一步应该补齐：

- 生命周期
- 状态切换规则
- 置信度/稳定度管理
- 剔除策略

### P2：中期方向

#### 6. 把对象真正并入后端联合优化

也就是后续可以考虑：

- 不再只做 `OptimizeLocalObjects`
- 而是把对象位姿作为图优化变量
- 和关键帧位姿、地图点一起进入更完整的局部优化框架

这一步价值很高，但复杂度也明显更高，建议放在前端对象残差稳定之后。

#### 7. 做静态对象的持续图像投影显示

当前图像侧只画当前帧观测框。

如果后续需要更强的调试和演示效果，可以考虑：

- 当前帧没有观测时
- 也把静态地图对象重新投影到相机图像上

这样能更清楚地区分：

- “检测没了”
- 还是“地图对象本身还在”

### P3：更长期和更系统化的方向

#### 8. 对象质量评估与数据集回归基线

建议逐步建立：

- 对象观测层指标
- 对象地图层指标
- 相机位姿指标
- 长序列回归脚本

原因：

- 没有系统化指标，后面每加一层对象逻辑都很难客观判断收益

## 七、当前阶段一句话总结

相比昨天“先验证对象可视化”的目标，今天实际上已经把系统推进到了：

- 对象进入地图
- 静态对象可持久化
- 基础对象优化接通
- mp4 中对象朝向关键 bug 已定位并修复

接下来最重要的不是继续盲目加新功能，而是：

- 先做 1200 帧完整回归
- 统一输入源
- 把对象链路收益和主 SLAM 精度变化彻底拆开验证清楚
